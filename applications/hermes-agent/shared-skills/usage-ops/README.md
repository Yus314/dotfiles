# Usage operations shared skills

Shared local-accounting and context-efficiency procedures for the `default`,
`career`, `english`, `indiedev`, and `researcheval` profiles.

The group contains generic diagnostics only. Profile database paths, recurring
review ownership, domain policy, and user-specific outcome criteria remain in a
separately named profile-local adapter.
