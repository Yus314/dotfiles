;;; selection-batch-core.el --- Selection batch state -*- lexical-binding: t; -*-

;;; Commentary:
;; State and selection model for selection-batch.

;;; Code:

(provide 'selection-batch-core)
;;; selection-batch-core.el ends here
