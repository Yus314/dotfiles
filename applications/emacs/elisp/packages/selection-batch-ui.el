;;; selection-batch-ui.el --- Selection batch user interface -*- lexical-binding: t; -*-

;;; Commentary:
;; User interface for selection-batch.

;;; Code:

(provide 'selection-batch-ui)
;;; selection-batch-ui.el ends here
