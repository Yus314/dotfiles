;;; selection-batch-operators.el --- Selection batch operators -*- lexical-binding: t; -*-

;;; Commentary:
;; Text operators for selection-batch.

;;; Code:

(provide 'selection-batch-operators)
;;; selection-batch-operators.el ends here
